import { generateUuid } from '../../../helpers/generate-uuid';
import { module, test } from 'qunit';

module('Unit | Helper | generate uuid');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = generateUuid(42);
  assert.ok(result);
});
