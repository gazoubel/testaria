<h1>{{t 'product.basic.commonWords.newTransaction'}}</h1>
{{form-transaction onSave='save' onCancel='cancel'
purchaseTransaction = model.purchaseTransaction
project = model.project itemTypes=model.itemTypes
itemTypes=model.itemTypes
paymentTypes = model.paymentTypes
providers = model.providers}}
